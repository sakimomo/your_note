<%= turbo_frame_tag dom_id(@user, :relationship) do %>
    <% if @user.followed_by?(current_user) %>
      <%= link_to "Following", user_relationships_path(@user.id), data: { turbo_method: :delete } %>
    <% else %>
      <%= link_to "＋Follow", user_relationships_path(@user.id), data: { turbo_method: :post } %>
    <% end %>
<% end %>