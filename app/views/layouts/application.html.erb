<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>your note</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%= javascript_include_tag 'hamburger_menu' %>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Arvo&family=Kanit:ital@1&family=M+PLUS+1p&family=Zen+Maru+Gothic:wght@300&display=swap" rel="stylesheet">

  </head>

  <body>
   <header class="header">
 <div class="logo">
    <%= link_to root_path do %>
    <%= image_tag("IMG_1878.jpg") %>
    <% end %>
  </div>
  <div class="menu-trigger">☰</div>
  <div class="menu">
    <div class="inner">

      <% if user_signed_in? %>
        <div class="nav__right">
          <%= link_to "New Post", new_note_path, class: :nav__btn %>
          <%= link_to "My Page", user_path(current_user), class: :nav__btn %>
          <%= link_to "Log Out", destroy_user_session_path, data: { turbo_method: :delete }, class: :nav__btn %>
        </div>
      <% else %>
        <div class="nav__right">
          <%= link_to "Log in", new_user_session_path, class: :nav__btn %>
          <%= link_to "Sign up", new_user_registration_path, class: :nav__btn %>
        </div>
      <% end %>
    </div>
  </div>
</header>
<%= yield %>
<footer class="footer">
 <p>&copy; 2024 Saki Kato. All Rights Reserved.</p>
</footer>
  </body>
</html>
